# Як розмістити сайт в Інтернеті (Deploy Guide)

Щоб вашим сайтом міг користуватися замовник, його потрібно завантажити на сервер ("хостинг"). Найпростіший і безкоштовний варіант для такої системи — **Render.com**.

Це трохи складніше, ніж просто натиснути кнопку, але я розписав покрокову інструкцію.

## Крок 1: Підготовка GitHub

Сервер буде брати код з GitHub.

1. Зареєструйтеся на [github.com](https://github.com/), якщо ще не маєте аккаунту.
2. Створіть новий **Repository** (сховище для коду). Назвіть його `marusZp`. Зробіть його **Private** (щоб ніхто чужий не бачив код).
3. На вашому комп'ютері відкрийте термінал (в цій папці) і виконайте команди (по одній):

```bash
git init
git add .
git commit -m "First commit"
git branch -M main
git remote add origin https://github.com/ВАШ_НІКНЕЙМ/marusZp.git
git push -u origin main
```

*(Замініть `ВАШ_НІКНЕЙМ` на ваш логін GitHub. Вас можуть попросити ввести логін і пароль/токен).*

## Крок 2: Реєстрація на Render

1. Зайдіть на [render.com](https://render.com/).
2. Натисніть **Sign Up** -> **GitHub**.

## Крок 3: Створення Бази Даних (PostgreSQL)

Сайт не може "запам'ятовувати" дані просто в повітрі, йому потрібна база.

1. У Render Dashboard натисніть **New +** -> **PostgreSQL**.
2. Налаштування:
    - **Name**: `marusZp-db`
    - **Region**: Frankfurt
    - Натисніть **Create Database**.
3. Коли вона створиться, знайдіть розділ **Connections** і скопіюйте **Internal Database URL** (воно починається на `postgres://...`). **Збережіть його!**

## Крок 4: Запуск сервера (Backend)

Це "мізки" сайту.

1. У Render Dashboard натисніть **New +** -> **Web Service**.
2. Виберіть ваш репозиторій `marusZp`.
3. Налаштування:
    - **Name**: `marusZp-backend`
    - **Region**: Frankfurt
    - **Branch**: `main`
    - **Root Directory**: `backend`
    - **Environment**: Python 3
    - **Build Command**: `pip install -r requirements.txt`
    - **Start Command**: `gunicorn main:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:10000`
4. **Environment Variables** (Важливо!):
    - Натисніть **Advanced** -> **Add Environment Variable**.
    - **Key**: `DATABASE_URL`
    - **Value**: Вставте те довге посилання `postgres://...`, яке ви скопіювали в Кроці 3.
5. Натисніть **Create Web Service**.
6. Зачекайте запуску. Скопіюйте посилання на сервіс (наприклад `https://marusZp-backend-xv32.onrender.com`).

## Крок 5: Запуск сайту (Frontend)

Це те, що бачите ви і клієнт.

1. У Render Dashboard натисніть **New +** -> **Static Site**.
2. Знову виберіть репозиторій `marusZp`.
3. Налаштування:
    - **Name**: `marusZp-frontend`
    - **Root Directory**: `frontend`
    - **Build Command**: `npm run build`
    - **Publish Directory**: `dist`
4. **Environment Variables** (Дуже важливо!):
    - Натисніть **Advanced** -> **Add Environment Variable**.
    - **Key**: `VITE_API_URL`
    - **Value**: Вставте посилання з Кроку 4 (наприклад, `https://marusZp-backend-xv32.onrender.com`).
5. Натисніть **Create Static Site**.

## Готово! 🚀

Коли Frontend добудується, Render дасть вам посилання (наприклад, `https://marusZp-frontend-ab12.onrender.com`).
Відправте це посилання замовнику. Він зможе заходити з телефону чи комп'ютера.

---
**Важливо:** Безкоштовний сервер на Render "засинає", якщо ним не користуватися 15 хвилин. Перший запуск після паузи може зайняти до 50 секунд. Це нормально.
